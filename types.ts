/**
 * Root type barrel — kept for backward compatibility.
 * All imports from 'types' or '../types' continue to work unchanged.
 *
 * New code should import directly from 'types/models', 'types/enums',
 * 'types/constants', or 'types/utils' for better tree-shaking.
 */
export * from './types/index';

