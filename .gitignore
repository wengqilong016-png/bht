# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
manage_users.js
.vercel

# Environment files — keep only .env.example in VCS
.env*
!.env.example

# Lock files — project uses npm (package-lock.json)
pnpm-lock.yaml

# Backup and export files — avoid committing sensitive data dumps
# Both *backup* and *BACKUP* are listed to cover case-sensitive Linux filesystems.
*backup*
*BACKUP*
*.backup.json
*_credentials*
*.dump
*.sql.gz
*.sqlite
*.db
*.jsonl
*.ndjson

# Build caches and local tooling
supabase/.temp/
coverage/
