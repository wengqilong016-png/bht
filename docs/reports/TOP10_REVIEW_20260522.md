# Top10 风险复核状态表（2026-05-22）

基线来源：`docs/reports/SOURCE_AUDIT_20260522.md` 的 Top10 待修复项。  
复核方式：代码取证 + 目标库 SQL 验收（H1/H5/H2-H4/H22 已执行验证）。

| ID | 原风险 | 当前状态 | 代码证据 | 结论与下一动作 |
|---|---|---|---|---|
| H1 | Realtime 广播全行数据 | 已在目标库验证通过 | `supabase/schema.sql:2691`、`supabase/schema.sql:2739`、`supabase/migrations/20260522000006_harden_realtime_broadcast.sql:24` | 已改为 driver 仅可订阅 `db:transactions:{driverId}`，并限制 `realtime.messages` topic 访问；目标库验收通过。 |
| H5 | evidence/kiosk-photos 存储桶公开 | 已在目标库验证通过 | `supabase/schema.sql:29`、`supabase/schema.sql:112`、`supabase/migrations/20260522000007_make_storage_buckets_private.sql:21` | bucket 为 `public=FALSE` 且 SELECT policy 生效；目标库验收通过。 |
| H6 | NaN 传播污染金融字段 | 已修复 | `services/collectionSubmissionService.ts:35`、`services/collectionSubmissionService.ts:220` | 已引入 `safeNumber` 并在金融字段映射中统一使用。建议：补一条单测覆盖 `NaN/Infinity/null` 返回 fallback。 |
| H10 | 离线队列跨标签页无保护 | 已修复（有残余风险） | `offlineQueue.ts:672`、`offlineQueue.ts:868` | 已加入 `BroadcastChannel` 协调与 `_otherTabFlushing` 短路；不再是“完全无保护”。残余：不支持 `BroadcastChannel` 的环境仍退化为单标签页互斥。 |
| H11 | IDB 事务自动提交导致循环重处理 | 已修复 | `offlineQueue.ts:517`、`offlineQueue.ts:546`、`offlineQueue.ts:1046`、`offlineQueue.ts:1540` | 关键路径均改为在同一 promise 中完成 `get+put`，避免事务失活。建议：补回归测试模拟多次 `markSynced/recordRetryFailure`。 |
| H8 | submit_collection_v2 客户端 driverId IDOR | 已修复 | `supabase/schema.sql:1973` | RPC 已校验 driver 调用者 `driver_id` 必须等于 `p_driver_id`，不匹配直接 `42501`。 |
| H18 | 收款拍照无压缩 | 已修复 | `driver/components/ReadingCapture.tsx:88`、`driver/components/QuickCollect.tsx:395` | 两处都已使用 `compressAndResizeImage(file)`，失败才回退原图。 |
| H2-H4 | 三个审计表任意 INSERT | 已在目标库验证通过 | `supabase/migrations/20260522000005_tighten_audit_table_insert_policies.sql:20`、`supabase/schema.sql:2987`、`supabase/schema.sql:2992` | `support_audit_log_insert` / `tx_audit_insert` / `amount_validation_audit_insert` 均已按预期收紧并通过目标库验收。 |
| H22 | reviewSettlement 非原子 | 已在目标库验证通过 | `hooks/useSupabaseMutations.ts:470`、`supabase/schema.sql:1585` | 前端已移除审核后的二次 `updateDriverCoins` 写入；`review_daily_settlement_v1` 内含 driver coins 更新，目标库验收通过。 |
| H15 | 客户端 OpenAI key 存储常量 | 误报/已过期 | `types/constants.ts:1`、`rg OPENAI_KEY_STORAGE`（无结果） | 常量已不存在，仓库无引用。建议：在审计文档标注“已过期线索”，避免重复追踪。 |

## 汇总

- 待修复：无
- 已在目标库验证通过：`H1`、`H5`、`H2-H4`、`H22`
- 已修复（代码侧）：`H6`、`H8`、`H10`、`H11`、`H18`
- 误报/过期：`H15`

## 建议修复顺序（下一轮）

1. 补一条 `H22` 回归测试：确认 review 成功时不会触发客户端二次写库
2. 为 `H10` 增加无 `BroadcastChannel` 环境的回归测试
